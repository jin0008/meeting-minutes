# 회의록 자동 생성기 - 필수 패키지

# 음성 녹음 (마이크 직접 녹음 시 필요)
sounddevice

# Whisper STT (음성→텍스트)
openai-whisper

# 화자 인식 (목소리 등록 및 발언자 식별)
resemblyzer

# 오디오 로드 (resemblyzer 의존)
librosa

# numpy (수치 연산)
numpy

# ── LLM API (둘 중 사용할 것만 설치해도 됩니다) ──

# Claude API 사용 시
anthropic

# Gemini API 사용 시
google-generativeai
